platform: template
switches:
  tv:
    friendly_name: "Television"
    value_template: "{{ is_state('binary_sensor.television', 'on') }}"
    turn_on:
      service: shell_command.tv_power
    turn_off:
      service: shell_command.tv_power

